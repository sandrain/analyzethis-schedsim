#!/usr/bin/perl
#

use strict;

my $cmd = "./sim.py -c 1 -n 4 --netbw 44566000000 --scheduler schedlib --placement schedlib /home/gvh/src/git/analyzethis-workflows/libconfig2xml/montage.job.xml";

system ($cmd);
