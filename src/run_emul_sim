#!/usr/bin/perl
#

use strict;

my $cmd;
my $arg = shift;

if ($arg eq "info") {
    $cmd = "./sim.py -e info -c 1 -n 4 --netbw 44566000000 --scheduler schedlib --placement schedlib /home/gvh/src/git/analyzethis-workflows/libconfig2xml/montage.job.xml";
} else {
    $cmd = "./sim.py -c 1 -n 4 --netbw 44566000000 --scheduler schedlib --placement schedlib /home/gvh/src/git/analyzethis-workflows/libconfig2xml/montage.job.xml";
}

print "Executing: $cmd\n";
system ($cmd);
